branches:
  only: master
addons:
  apt:
    packages:
    - python3-requests
    - python3-yaml
before_script: chmod +x amy_add.sh
script: "./amy_add.sh"
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
